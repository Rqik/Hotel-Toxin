//- title -- text description
//- items -- Array<string> text item
mixin bulletList({title , items})
  .bullet-list
    if title
      .bullet-list__title=title
    ul.bullet-list__items
      each item in items
        li.bullet-list__item=item
